# PluginHub

## 说明
这是用于让可莉插件中心的部分插件以 `Surge` 模块方式导入的适配模块。

使用前需要先导入 `Script-Hub` 模块。

## 使用方式
- 在 `Surge` 中导入 `pluginhub.sgmodule`。
- 打开可莉插件中心页面后，按模块说明继续导入对应插件。

## 来源
- https://raw.githubusercontent.com/Yu9191/Rewrite/refs/heads/main/pluginhub.sgmodule
