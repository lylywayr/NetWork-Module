# 服务器监控

## 说明
这是 `Egern` 的服务器监控小组件目录。

## 使用方法
- 在 `Egern` 中导入 `ServerMonitor.yaml`。
- `yaml` 已经指向当前目录内的 `ServerMonitor.js` 原始链接。

## 上游模块地址
- `Egern/Widgets/ServerMonitor.yaml`

## 上游脚本地址
- `Egern/Widgets/ServerMonitor.js`
