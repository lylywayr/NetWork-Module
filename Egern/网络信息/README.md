# 网络信息

## 说明
这是 `Egern` 的网络信息小组件目录。

## 使用方法
- 在 `Egern` 中导入 `NetworkInfo.yaml`。
- `yaml` 已经指向当前目录内的 `NetworkInfo.js` 原始链接。

## 上游模块地址
- `Egern/Widgets/NetworkInfo.yaml`

## 上游脚本地址
- `Egern/Widgets/NetworkInfo.js`
