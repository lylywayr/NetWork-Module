# Stash

## 说明
这是 `Stash` 客户端目录。

当前保留明确属于 `Stash` 的中性覆写脚本，并按功能拆成中文目录。

## 当前模块
- `Google重定向`
- `京东比价`
- `TestFlight账户管理`
- `TestFlight区域修正`

## 维护说明
- 模块目录内保留 `.stoverride`、依赖脚本和 `README.md`。
- 覆写文件里的脚本链接已改为当前仓库的原始链接。
